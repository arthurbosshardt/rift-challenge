/** Shared mobile/desktop layout breakpoint (matches SCSS `max-width: 1023px`). */
export const MOBILE_MAX_WIDTH_PX = 1023;

export const MOBILE_MEDIA_QUERY = `(max-width: ${MOBILE_MAX_WIDTH_PX}px)`;
